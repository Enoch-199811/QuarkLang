func work(n int) int {
    s int = 0;
    i int = 0;
    while (i < 100000) {
        s = s + i;
        i = i + 1;
    }
    return s + n;
}
func main(io IOStream) {
    t thread = taskm.spawn();
    t.merge(work, 1);
    taskm.block(t.pid());
    io.println(taskm.done(t.pid()));
}
