# QuarkLang v2 定案模型（2026-08-30 汇总，待设计者最终确认）

> 本文档汇总近期全部设计决定，是整体重写的依据。标注 ⚠ 者为尚未定死的点。

## 1. 类型体系（FuncBuffer 彻底删除）

- `void` = 空接口 `interface{}` 的**默认名字**（语言预声明 `interface {} void;`；"不是空，是没有接口"）。以后可直接写 `void` 表示空接口。
- `interface { ... }`：**匿名接口**（可作类型标注）；`interface { ... } 名字;`：有名接口。
- `struct { ... }`：**匿名结构体类型**；`struct { ... } 名字;`：有名结构体。
- 结构体字面量：`.{field: value, ...}`（字段名允许 `in`/`out` 等关键字）。
- `impl [接口] { ... } 类型;`：有名实现；`impl { ... } 类型;`：匿名实现。
- 泛型 `struct<T>`/`impl<T>`（struct 有泛型参数时 impl 必须引入）；指针 `T&` 与 `null`（自动解引用）；`Copyd<T>`（传递复制，`.ptr()` 取包装值）。以上保留。
- `List<T>`：滚动双指针缓冲（`*` 取开头、`next()`、`head()==tail()` 耗尽报错、`reset()` 回卷）。

## 2. 函数

- **必须声明返回类型**：`func f(n int) int { ... }`（main 可省略 = void）。
- `return expr;`：返回并结束函数。
- `log expr;`：记录日志并结束函数（返回任意值，即 void 亦可）。
- **无 out、无多返回值**；多输出用 `List<T>` 作为单一返回值。调用直接得值。

## 3. 错误处理

- `try { ... } catch (名字 类型) { ... }`：catch 必须写名字和类型（如 `catch (e void)`）；错误信息装入 e。

## 4. 签名（Sign）

- `f(args) @mb(prefix)` ≡ `mb.call(prefix)(.{in: ..., out: ...})`：
  - mb 是 Sign 实例（`mb 类名 = 类名::new();`；类实现 `impl Sign {...} 类名;`）；
  - `mb.call(prefix)` 输出一个**函数**，该函数接收 `.{in, out}` 两字段记录并返回结果；
  - memorize 按 in→out 记忆化。
- ⚠ c. call 返回的函数如何执行被包装的原函数 f（f 引用从哪来）——待定。

## 5. 任务（taskm 全局变量）

- `taskm.spawn()`：参数为空，创建线程并直接返回 pid。
- `taskm.merge(pid, fn, args...)`：把函数并入线程 pid 执行。
- `taskm.block(pid)`：返回 void（空接口）——只等待，结果另走通道。
- `taskm.done(pid)`：该线程是否空闲（没有函数占用）。
- `taskm.channel([n])`：默认容量 1024；IO 执行表 FIFO、读优先。

## 6. 内存

- block 分配（`memory.setBlock(n)` 调粒度）、脏标记、协程结束自动标记可回收、`compact()` 实际清理（不返回）。

## 7. 宏系统（新）

- 定义：`macro {模式} {主体}`——模式支持**任意形式**（如 `macro {program ...} {}`），但**模式内不能再套大括号**。
- 调用：按宏自身的模式匹配源码形态。
- 主体（第二对大括号）为**动态预处理代码**，`#` 开头为预处理命令：
  - `#when (compile) { ... }` / `#when (run) { ... }`：按编译期/运行期条件执行；
  - `#insert(#ast(符号))`：把符号的 AST 插入当前输出；
  - `#execute(符号)`：运行期执行；
  - `#error("...") `：报错。
- **预制宏**：
  - `program main;`：包装整个程序为可运行程序（否则编译/运行结果为空）；
  - `program library;`：编译为库（不可运行，`#when (run) { #error("cannot run a library") }`）；
  - `pub`：前缀于函数/数值/符号，在库中公开；
  - `import ...;`：按编译/运行选项寻找 imports（同目录默认在搜索范围内）。
- 预处理是**动态**的（编译期与运行期两态）。

## 8. 待确认

1. ⚠ c. 签名 call 如何执行被包装函数 f。
2. 宏系统属于本次 v2 整体重写范围，还是 v2 核心先落地、宏系统随后一轮实现？
